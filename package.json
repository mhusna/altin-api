{
  "type": "module",
  "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "deploy": "git pull origin main && npx tsc && pm2 restart altin-api"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "typescript": "^5.9.3",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^20.0.0"
  }
}
